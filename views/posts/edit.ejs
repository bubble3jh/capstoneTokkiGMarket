<!DOCTYPE html>
<html>

<head>
  <%- include('../partials/head') %>
</head>
<style>
  body {
    background-color: #FFDEB8;
    padding: 50px;
  }

  .post_show_title {
    background-color: #ffffff;
    position: relative;
    padding: 50px 100px 100px 395px;
    height: 600px;
  }

  .post_show_title .post_show_img {
    position: absolute;
    left: 100px;
    top: 100px;
  }

  .post_show_title .post_show_img>img {
    height: 400px;
    width: 400px;
    border: 1px solid #e8e8e8;
  }

  .post_show_title>h2 {
    margin: 0 100px 15px;
    padding: 0 100px 20px;
    border-bottom: 2px solid #333;
    font-size: 24px;
    color: #232323;
    line-height: 26px;
  }

  .post_show_title table th,
  .post_show_title table td {
    padding: 20px 200px;
    font-size: 18px;
    text-align: left;
  }

  .post_show_title table th {
    font-size: 22px;
    color: #444;
    text-align: left;
  }

  .post_show_title table td {
    font-size: 20px;
    color: #444;
  }

  .post_show_title table td b {
    font-size: 22px;
    color: #444;
  }

  .post_show_title tbody {
    position: relative;
    right: 100px;
  }

  .chatbtn .btn {
    position: relative;
    right: -100px;
    bottom: 100px;
    display: inline-block;
    width: 136px;
    height: 42px;
    font-size: 16px;
    color: #fff;
    border-radius: 2px;
    background: #f78736;
  }
</style>

<body>
  <%- include('../partials/sim2_nav') %>
    <form action="/posts/<%= post._id %>?_method=put<%= getPostQueryString(true) %>" enctype="multipart/form-data"
      method="post">
      <div class="post_show_title">

        <h2>제목 <input type="text" id="title" name="title" value="<%= post.title %>">
          <% if(errors.title){ %>
            <span class="invalid-feedback">
              <%= errors.title.message %>
            </span>
            <% } %>
        </h2>
        <table>

          <colgroup>
            <col>
          </colgroup>
          <tbody>
            <tr>
              <th>상품 사진</th>
              <td><input type="file" name="attachment" class="form-control-file" id="attachment"></td>
            </tr>
            <tr>
              <th>가격</th>
              <td>
                <input type="text" id="price" name="price" value="<%= post.price %>"
                  class="form-control <%= (errors.title)?'is-invalid':'' %>">
                <% if(errors.price){ %>
                  <span class="invalid-feedback">
                    <%= errors.price.message %>
                  </span>
                  <% } %>
              </td>
            </tr>
            <tr>
              <th>상품상세</th>
              <td>
                <textarea id="body" name="contents" rows="5"
                  class="form-control <%= (errors.contents)?'is-invalid':'' %>"><%= post.contents %></textarea>
                <% if(errors.contents){ %>
                  <span class="invalid-feedback">
                    <%= errors.contents.message %>
                  </span>
                  <% } %>
              </td>
            </tr>
          </tbody>
        </table>

        <div>
          <button style="
            position:relative;
            left:850px;
            top:100px;
            height:45px;
            width: 120px;
            display: inline-block;
            font-size: 16px;
            color: #fff;
            border-radius: 2px;
            background: #f78736;"class="chatbtn" href="/posts<%= getPostQueryString() %>">이전</button>
          <button style="
            position:relative;
            left:850px;
            top:100px;
            height:45px;
            width: 120px;
            display: inline-block;
            font-size: 16px;
            color: #fff;
            border-radius: 2px;
            background: #f78736;"type="submit" class="chatbtn">작성완료</button>
        </div>

      </div>
    </form>
</body>

</html>